Please use the form below to sign our guestbook!

<?php
$form = $this->form;
$form->setAction($this->url('default', array('controller' => 'guestbook', 'action' => 'sign')))
     ->setView($this);
echo $form;
